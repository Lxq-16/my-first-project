<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.club.mapper.ClubMapper">

    <select id="selectClubWithTypePage" resultType="com.club.vo.ClubTypeVo">
        SELECT
        c.club_id id,
        c.club_name clubName,
        c.club_type typeId,
        c.manager_id leaderId,
        c.club_desc intro,
        c.status,
        c.create_time createTime,
        t.type_name typeName
        FROM club c
        LEFT JOIN club_type t ON c.club_type = t.id
        <where>
            c.is_deleted = 0
            <!-- 社团名称 -->
            <if test="query.clubName != null and query.clubName != ''">
                AND c.club_name LIKE CONCAT('%', #{query.clubName}, '%')
            </if>
            <!-- 分类ID -->
            <if test="query.typeId != null">
                AND c.club_type = #{query.typeId}
            </if>
            <!-- 社团状态 -->
            <if test="query.status != null">
                AND c.status = #{query.status}
            </if>
            <!-- 跨表 分类名称模糊 -->
            <if test="query.typeName != null and query.typeName != ''">
                AND t.type_name LIKE CONCAT('%', #{query.typeName}, '%')
            </if>
            <!-- 社长ID 前端携带的搜索框 -->
            <if test="query.managerId != null">
                AND c.manager_id = #{query.managerId}
            </if>
        </where>
        ORDER BY c.club_id ASC
    </select>

    <!-- 手动count语句，解决MP分页子查询带order by语法报错 -->
    <select id="selectClubWithTypePage_COUNT" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM club c
        LEFT JOIN club_type t ON c.club_type = t.id
        <where>
            c.is_deleted = 0
            <if test="query.clubName != null and query.clubName != ''">
                AND c.club_name LIKE CONCAT('%', #{query.clubName}, '%')
            </if>
            <if test="query.typeId != null">
                AND c.club_type = #{query.typeId}
            </if>
            <if test="query.status != null">
                AND c.status = #{query.status}
            </if>
            <if test="query.typeName != null and query.typeName != ''">
                AND t.type_name LIKE CONCAT('%', #{query.typeName}, '%')
            </if>
            <if test="query.managerId != null">
                AND c.manager_id = #{query.managerId}
            </if>
        </where>
    </select>

</mapper>